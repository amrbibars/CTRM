<Window x:Class="CTRM.Budgeting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CTRM"
        mc:Ignorable="d"
        Title="Budgeting" Height="450" Width="800"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded"
        Background="{StaticResource AppWindowBackground}">

    <!-- 🔹 Make ALL buttons in this window use App.xaml's ButtonBaseStyle -->
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="0,0,0,10" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="0,0,0,10">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">

                <Button Content="Import Orders" Name="BtnImportOrders" Click="BtnImportOrders_Click" Padding="15,5" Margin="0,0,20,0"/>

                <Rectangle Width="1" Fill="Gray" Margin="0,5,20,5"/>

                <TextBlock Text="Start:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,5,0"/>
                <ComboBox x:Name="CmbStartMonth" Width="80" SelectionChanged="CmbStartMonth_SelectionChanged" Margin="0,0,15,0"/>

                <CheckBox x:Name="ChkNextYear" Content="Next Year(s)" VerticalAlignment="Center" Checked="ChkNextYear_CheckedChanged" Unchecked="ChkNextYear_CheckedChanged" Margin="0,0,15,0"/>

                <TextBlock Text="Roll To:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,5,0"/>
                <ComboBox x:Name="CmbRollMonth" Width="80" Margin="0,0,5,0"/>

                <TextBox x:Name="TxtRollYear" Width="60" MaxLength="4" IsEnabled="False" VerticalContentAlignment="Center" Margin="0,0,15,0" PreviewTextInput="NumberValidationTextBox"/>

                <Button Content="Forecast" Name="BtnForecast" Click="BtnForecast_Click" Padding="15,5" Margin="0,0,10,0"/>

                <Button Content="Export to Excel" Name="BtnExport" Click="BtnExport_Click" Padding="15,5" IsEnabled="False"/>
            </WrapPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock Text="Historical Data" DockPanel.Dock="Top" FontWeight="Bold" Margin="0,0,0,5"/>
                <DataGrid x:Name="OrdersGrid" 
                          AutoGenerateColumns="True" 
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          ColumnWidth="SizeToCells"
                          HorizontalAlignment="Stretch" 
                          GridLinesVisibility="Horizontal"
                          AutoGeneratingColumn="DataGrid_AutoGeneratingColumn"
                          AlternatingRowBackground="#F0F0F0"/>
            </DockPanel>

            <DockPanel Grid.Column="2">
                <TextBlock Text="Forecast Result" DockPanel.Dock="Top" FontWeight="Bold" Margin="0,0,0,5" Foreground="#00493E"/>
                <DataGrid x:Name="ForecastGrid" 
                          AutoGenerateColumns="True" 
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          ColumnWidth="SizeToCells"
                          HorizontalAlignment="Stretch"
                          GridLinesVisibility="Horizontal"
                          AutoGeneratingColumn="DataGrid_AutoGeneratingColumn"
                          AlternatingRowBackground="#E8F5E9"/>
            </DockPanel>
        </Grid>
    </Grid>
</Window>
